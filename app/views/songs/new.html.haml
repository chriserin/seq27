%section#song
:javascript
  Midi.connect();
  window.SONG_STATE = Song.newSong()
  ViewState.init(SONG_STATE)
  var view = React.createElement(SeqCom.Song, null)
  window.SONG_VIEW = ReactDOM.render(view, window.song)
